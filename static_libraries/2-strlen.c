#include "main.h"

/**
 * _strlen - A functions thar returns the lenght of a string
 * @s: The string to be checked.
 *
 * Return: The count.
 */
int _strlen(char *s)
{
	int count;

	for (count = 0; s[count] != 0; ++count)
	{
	}
	return (count);
}
