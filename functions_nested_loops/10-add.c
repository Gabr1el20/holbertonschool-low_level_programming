#include "main.h"

/**
 * add - adds two integers and returns the result.
 * @x: the first integer.
 * @y: the second integer.
 *
 * Return: the result of the add.
 */
	int add(int x, int y)
{
	return (x + y);
}

