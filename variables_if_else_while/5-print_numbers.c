#include <stdio.h>

/**
 *	main - Entry point
 *
 *	Return: Always 0 (success)
 *
 */

	int main(void)
{
	int numero;

	for (numero = 0; numero < 10; numero++)
		printf("%d", numero);
	printf("\n");
	return (0);
}
